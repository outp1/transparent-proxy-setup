base {
  log_debug = off;
  log_info = on;
  daemon = on;
  redirector = iptables;
}

redsocks {
  local_ip = 127.0.0.1;
  local_port = 12345;
  ip = {{PROXY_HOST}};
  port = {{PROXY_PORT}};
  type = {{PROXY_TYPE}};   // http-connect or socks5
  // login/password are optional
  {{AUTH_BLOCK}}
}
